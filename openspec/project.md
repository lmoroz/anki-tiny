# Project Context: Repetitio

## Purpose

**Repetitio** — десктопное приложение для обучения с помощью карточек и интервального
повторения, вдохновлённое Anki. Проект разрабатывается с использованием LLM-агентов в модели
**human-in-the-loop**: архитектурные решения и контроль качества осуществляет разработчик,
а рутинные задачи ускоряются с помощью AI.

### Основные цели

1. **Создание курсов** — система организации учебного материала по темам
2. **Быстрое добавление карточек** — удобный интерфейс для пополнения контента
3. **Гибкие настройки** — глобальные и индивидуальные для каждого курса
4. **Адаптивное расписание** — учёт времени дня для тренировок
5. **Системные уведомления** — напоминания о необходимости повторения
6. **Интеграция с системным треем** — сворачивание приложения вместо закрытия
7. **Кастомизированный FSRS** — настраиваемые интервалы повторения (например, первый интервал = 4 часа)

---

## Tech Stack

### Frontend

- **Vue 3** (v3.5+) — Composition API, `<script setup>`
- **Vue Router** (v4.6+) — Hash mode для кастом протокола
- **Vite** (v7.2+) — Build tool и dev server
- **TailwindCSS** (v4.1+) — Utility-first CSS framework
- **Pinia** (v3.0+) — State management
- **Axios** (v1.13+) — HTTP клиент
- **@vueuse/core** (v14.1+) — Composition утилиты
- **Bootstrap Icons** — Иконки
- **ts-fsrs** (v5.2+) — Библиотека для spaced repetition

### Backend

- **Node.js** (>=18.0.0) + **TypeScript** (v5.9+)
- **Express** (v4.18+) — Web framework
- **Electron** (v39.2+) — Desktop wrapper
- **Better SQLite3** (v12.5+) — База данных
- **Kysely** (v0.27+) — Type-safe SQL query builder
- **Zod** (v4.1+) — Валидация схем
- **Pino** (v10.1+) — Логирование
- **fs-extra** (v11.2+) — Расширенные операции с файлами

### Tooling

- **ESLint** + **Prettier** — Code quality и форматирование
- **npm workspaces** — Монорепозиторий (frontend + backend)
- **electron-builder** — Создание установщиков
- **concurrently** — Параллельный запуск процессов

---

## Project Conventions

### Code Style

#### Форматирование

- **Отступы**: 2 пробела (согласно `.editorconfig`)
- **Кодировка**: UTF-8
- **Переводы строк**: Unix-style (LF)
- **Trailing whitespace**: удалять
- **Финальный перевод строки**: обязателен
- **Максимальная длина строки**: 160 символов для кода, 120 для markdown

#### Условные конструкции (if/else)

- Открывающая скобка `{` всегда на той же строке
- `else` и `else if` всегда на новой строке
- Для одного statement скобки НЕ используются (однострочный вариант)
- Для нескольких statements скобки ОБЯЗАТЕЛЬНЫ

**Правильно:**

```ts
if (condition) doSomething();
else doSomethingElse();

if (condition) {
  doFirstThing();
  doSecondThing();
}
else doOneThing();
```

#### Импорты (Frontend)

- **ВСЕГДА** использовать alias `@` для импортов из `src/`
- **ЗАПРЕЩЕНО** использовать относительные пути (`../`, `./`)
- **НЕ импортировать** Vue-компоненты вручную (используется `unplugin-vue-components`)
- Импорты только для: composables, утилит, внешних библиотек

**Правильно:**

```ts
import {useApiClient} from '@/shared/api/client';
```

**Неправильно:**

```ts
import {useApiClient} from '../shared/api/client';
import BaseButton from '@/components/BaseButton.vue'; // компоненты автоимпорт
```

#### Структура Vue-файлов

- `<script setup>` блок всегда **выше** `<template>` блока

---

### Architecture Patterns

#### Frontend: Feature-Sliced Design (FSD)

Проект организован по методологии FSD:

- **app/** — инициализация приложения, роутинг
- **pages/** — страницы (home, course, training, settings)
- **widgets/** — составные UI блоки (title-bar, course-list, card-editor)
- **features/** — бизнес-фичи (create-course, add-card, spaced-repetition)
- **entities/** — бизнес-сущности (course, card, settings)
- **shared/** — переиспользуемый код (ui, api, lib, types)

#### Backend: Layered Architecture

- **electron/** — Electron main process (main.ts, preload.ts)
- **routes/** — API endpoints
- **controllers/** — Request handlers (если потребуется)
- **services/** — Бизнес-логика (database, repositories, FSRS)
- **schemas/** — Zod validation

#### Ключевые паттерны

- **Singleton** для Database инстанса
- **Repository Pattern** для работы с БД
- **Custom Protocol** (`lmorozanki://`) для загрузки ресурсов
- **IPC Communication** через Electron preload API
- **Migration System** с таблицей `_migrations` для отслеживания версий БД

---

### Testing Strategy

#### Текущий подход

- **Ручное тестирование** через DevTools Console (для API)
- **Функциональное тестирование** в Electron окне
- **Code Review** AI-сгенерированного кода
- **TypeScript** как первая линия защиты от ошибок

#### Планируемый подход

- **Vitest** для unit-тестов (уже настроен в backend)
- **API Integration Tests** для проверки endpoints
- **E2E тесты** для критичных user flows (опционально)

---

### Git Workflow

#### Стратегия коммитов

- **Semantic Commits**: `type(scope): subject`
- Примеры типов: `feat`, `fix`, `chore`, `docs`, `refactor`
- Scope указывает на модуль: `frontend`, `backend`, `docs`, `db`

**Примеры:**

```text
feat(frontend): add QuickAddCard component
fix(backend): correct course API route conflict
chore(deps): bump version to 0.2.1
docs(readme): update installation instructions
```

#### Workflow процесс

1. **Планирование** — задачи описываются в `docs/Task.md`
2. **Разработка** — AI-агент генерирует код по спецификации
3. **Code Review** — разработчик проверяет и корректирует
4. **Linting** — автоматические проверки через ESLint/Prettier
5. **Testing** — функциональное тестирование изменений
6. **Documentation** — синхронизация `docs/Walkthrough.md`, `docs/Changelog.md`
7. **Commit** — семантический коммит с версионированием (SemVer)

---

## Domain Context

### Система интервального повторения

Проект использует **FSRS (Free Spaced Repetition Scheduler)** через библиотеку `ts-fsrs`:

- Пользовательская кастомизация интервалов
- Первый интервал: 4 часа (вместо стандартных суток)
- Учёт времени дня для тренировок
- **Ограничение**: не предлагать новые карточки, если до конца дня < 4 часов

### Структуры данных

#### Course (Курс)

```typescript
interface Course {
  id: string;
  name: string;
  description: string;
  cardsCount: number;
  dueCardsCount: number;
  settings?: CourseSettings;
  createdAt: Date;
  updatedAt: Date;
}
```

#### Card (Карточка)

```typescript
interface Card {
  id: string;
  courseId: string;
  front: string;
  back: string;
  easinessFactor: number;
  interval: number;
  repetitions: number;
  dueDate: Date;
  createdAt: Date;
  lastReviewedAt?: Date;
}
```

#### Settings (Настройки)

```typescript
interface Settings {
  trainingStartHour: number; // 8 по умолчанию
  trainingEndHour: number; // 22 по умолчанию
  minTimeBeforeEnd: number; // 4 часа
  notificationsEnabled: boolean;
}
```

---

## Important Constraints

### Технические

1. **Windows-совместимость** — основная ОС для разработки и тестирования
2. **Encoding в терминале** — требуется установка UTF-8 для корректного отображения кириллицы
3. **Better SQLite3** — требует пересборки нативного модуля через `electron-rebuild`
4. **Frameless окно** — кастомный title bar с backdrop blur (Acrylic на Windows 11)
5. **Tray Integration** — приложение сворачивается в трей, а не закрывается

### Бизнес-логика

1. **Время тренировок** — ограничение по времени дня (`trainingStartHour` до `trainingEndHour`)
2. **Интервал повторения** — первый шаг = 4 часа (требует минимум 4 часа до конца дня)
3. **Системные уведомления** — необходимость интеграции с Windows/Linux/macOS

### Процессные

1. **AI-Assisted Development** — весь код генерируется AI, но контролируется человеком
2. **Russian Language** — вся коммуникация и комментарии на русском языке
3. **No .gitignore edits** — не редактировать файлы, указанные в `.gitignore`
4. **Pre-commit validation** — всегда выполнять `npm run lint` перед коммитом

---

## External Dependencies

### MCP серверы

- **Context7** — доступ к актуальной документации библиотек (TailwindCSS v4, Vue 3, Electron, etc.)

### Внешние библиотеки (критичные)

- **ts-fsrs** — алгоритм интервального повторения
- **better-sqlite3** — нативный модуль для SQLite
- **electron** — desktop wrapper

### Системные интеграции

- **Windows Notifications API** — системные уведомления
- **Electron Tray API** — интеграция с системным треем
- **Custom Protocol Handler** — `lmorozanki://` для загрузки ресурсов

---

## Project Status

### Реализовано ✅

- Полная структура проекта (FSD для frontend, Layered для backend)
- Кастомный Title Bar с управлением окном
- Database Layer (SQLite + Kysely) с системой миграций
- Courses API (CRUD endpoints)
- Frontend интеграция курсов (Pinia stores, UI components)
- QuickAddCard компонент для быстрого добавления карточек
- Light/Dark темы с Tailwind CSS v4
- Система настроек (глобальные + индивидуальные для курсов)

### В работе 🔄

- Cards API и интеграция с FSRS
- Training Page с алгоритмом повторения

### Запланировано 📅

- Системные уведомления
- Tray Integration
- Статистика прогресса обучения
- Импорт/Экспорт курсов
- Поддержка медиа в карточках (изображения, аудио)
